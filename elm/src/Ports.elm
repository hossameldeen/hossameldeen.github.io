port module Ports exposing (..)

-- There're ports in `Routing.elm` as well!
-- There're ports in `Routing.elm` as well!
-- There're ports in `Routing.elm` as well!
-- There're ports in `Routing.elm` as well!
-- There're ports in `Routing.elm` as well!
-- There're ports in `Routing.elm` as well!
-- Since there're nothing yet in the `Ports` to compile, but I want to keep the module to at least document the ports.


port dummyPort : String -> Cmd msg
